技能分發雙軌 (TASK-028 + 2026-09-21 借鏡 jl): profile skills 是「複製」非 symlink。委派/QA 流程類 skill(vpos-delegation-workflow、refactoring-qa-review)已升到 global(~/.hermes/skills/)→所有 profile 自動共用、換專案零摩擦；但**仍同步 cp -r 到 vpos_core/ui/qa profile/skills/**(與 global byte-identical,因 local>global 優先級故無衝突)。custom skill 更新後:先改 source-of-truth(vpos_lead),再 cp -r 到其餘 3 profile + global,ls 驗證三者一致。concurrency-audit/sqlite-connection-string-audit→core+qa;UI 類(proxy-property-cleanup/extract-static-to-singleton)→ui+qa。分發後 ls 驗證 + Hermes_BK 備份。
§
代理屬性清理 (TASK-014): 命名 m_XXX→mXxx(保留 m 前綴 camelCase)；全大寫縮寫視為單字不拆(m_ClassLastTime→mClassLastTime)。stub 刪除順序:(1)外部 cascade→(2)內部直接存取→(3)獨立 grep 驗證→(4)刪存根→(5)最終驗證。每次只刪一個 stub(fuzzy 大量空白會失敗)。內部引用 \bm_xxx\s*= / \bm_xxx\. 全改 MainWindowState.Instance.mXxx。完成後必通知使用者編譯驗收，不可連續多 Task。TASK-014 完成後每次編譯通過必 graphify update .。
CS 教訓: TASK-010 Connection Pooling 必附「所有 _sharedConnection.Query→GetSharedConnection().Query」。HttpClient 子代理易遺漏 setHeader()，必附 HttpRequestMessage+SendAsync。CS0136: #if DEBUG 內多處 String StrLog→inline LogFile.Write()。
§
TASK-029 (DONE+QA PASS): static List 執行緒安全化=消除原地 mutate+torn read(非加鎖)。55分三類:A真hazard→修掉;B View-local→取消零增益高風險;C atomic reassignment(原子指派天生 torn-read-safe)→取消。派單前必做跨線风险分析(全grep引用是否超出 Views/Thread/)。範圍調整 docs/plans/TASK-029-scope-adjustment.md(QA 報告只能 vpos_qa 產出放 review-reports/)。
§
TASK-032/033 教訓 (多執行緒修補): (1) DoD grep 勿用 `&&` 串接——`grep -c pat file` 計數為0時回傳exit code 1會中斷整條鏈。改用`;`分隔或各自獨立、每項印數字。(2) task_board.json 可能被子代理寫成 minified(單行JSON)，多行 patch fuzzy match 失敗 → 一律用 Python json.load→改欄位→json.dump(indent=2)，勿對 JSON 文字做 string matching。完整 SOP 見 skill vpos-delegation-workflow 陷阱41/42。(3) watchdog(cronjob_manage)與 QA(delegate_task)是兩個獨立工具——派完 delegate_task 後要建 cron watchdog，必須「直接調用 cronjob_manage」，絕不能把它塞進 delegate_task 的 goal（會產生假子代理、需再 stop）。